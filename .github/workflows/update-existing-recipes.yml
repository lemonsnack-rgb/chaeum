name: Update Existing Recipes

on:
  schedule:
    # 5분마다 실행 (GitHub Actions 최소 간격)
    # 무료 티어: 5분당 3개 기존 레시피 업데이트
    - cron: '*/5 * * * *'

  # 수동 실행 가능
  workflow_dispatch:
    inputs:
      batch_size:
        description: '업데이트할 레시피 개수'
        required: false
        default: '3'

  # 외부 cron 서비스에서 트리거
  repository_dispatch:
    types: [update-recipes]

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install dependencies
        run: npm install

      - name: Check environment variables
        run: |
          echo "Checking environment variables..."
          echo "VITE_GEMINI_API_KEY: ${VITE_GEMINI_API_KEY:0:10}..."
          echo "VITE_SUPABASE_URL: $VITE_SUPABASE_URL"
          echo "SUPABASE_SERVICE_ROLE_KEY: ${SUPABASE_SERVICE_ROLE_KEY:0:10}..."
          echo "UPDATE_BATCH_SIZE: $UPDATE_BATCH_SIZE"
        env:
          VITE_GEMINI_API_KEY: ${{ secrets.VITE_GEMINI_API_KEY }}
          VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          UPDATE_BATCH_SIZE: ${{ github.event.inputs.batch_size || '3' }}

      - name: Update existing recipes
        env:
          VITE_GEMINI_API_KEY: ${{ secrets.VITE_GEMINI_API_KEY }}
          VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          VITE_SUPABASE_ANON_KEY: ${{ secrets.VITE_SUPABASE_ANON_KEY }}
          VITE_UNSPLASH_ACCESS_KEY: ${{ secrets.VITE_UNSPLASH_ACCESS_KEY }}
          UPDATE_BATCH_SIZE: ${{ github.event.inputs.batch_size || '3' }}
        run: npm run update-recipes

      - name: Notify success
        if: success()
        run: |
          echo "✅ Recipes updated successfully!"

      - name: Notify failure
        if: failure()
        run: |
          echo "❌ Recipe update failed!"
